# Photography

The site's own photographs. Each file is named for the slot it fills; the
slots, their shapes and what belongs in them are listed in `tools/photos.py`,
which prints the ones still empty when you run it.

Anything not here yet stays on the template's stock image, so files can be
added a few at a time.
